import pandas as pd
import re

# Load raw data
df = pd.read_csv('data/telegram_data.csv')

# Example cleaning function
def clean_text(text):
    text = str(text)
    text = re.sub(r'[^\w\s፡።ሀ-፼]', '', text)  # remove unwanted symbols
    text = re.sub(r'\s+', ' ', text).strip()
    return text

df['cleaned_text'] = df['message'].apply(clean_text)
